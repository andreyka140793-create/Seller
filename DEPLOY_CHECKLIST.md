# Marginator — чеклист после деплоя (Amvera)

## Перед выкладкой
- [ ] requirements.txt: openpyxl, xlrd, python-docx, pypdf, dbfread, odfpy, pyxlsb
- [ ] .env: BOT_TOKEN, XAI_API_KEY (если нужен), WEBAPP_URL (HTTPS)
- [ ] Исходящий HTTPS (Telegram API)

## Золотой сценарий (5 минут)
1. /start — текст про 3 шага, клавиатура есть
2. Демо-прайс → режим Маркетплейс
3. Колонки → Последние/умолч. → порог 5% → Рассчитать
4. Итог + Excel (Справка/Все/Риск/Топ) + Mini App
5. Mini App: KPI, список, фильтры
6. К закупке ROI 30%
7. Новый файл с теми же параметрами
8. /terms
9. История → скачать; при 2+ — сравнение

## Если сломалось
| Симптом | Куда смотреть |
|---------|----------------|
| Бот молчит | токен, polling/webhook, логи |
| Mini App 401/пусто | WEBAPP_URL, initData, HTTPS |
| Файл не читается | текст ошибки, формат |
| Excel битый | openpyxl, размер |

## Регресс
После каждой выкладки — пункты 1–5 обязательно.
